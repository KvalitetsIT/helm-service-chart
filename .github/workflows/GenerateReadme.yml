name: Push commit
on:
  push:
    branches: [ master ]
    
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Create report file
        run: ./service/documentation/createReadme.sh
      - name: Commit report
        run: |
          git config --global user.name 'ReadmeBot'
          git add -A
          git commit -am "Automated report"
          git push
